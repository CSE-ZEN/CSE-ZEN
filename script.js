(()=>{const canonicalHtmlPages=new Set(['contact','equipe-reseau','formations','notre-offre','pole-avocats','pole-comptabilite','ressources','solutions-digitales']);const path=window.location.pathname;const lastSegment=path.split('/').filter(Boolean).pop()||'';const isTopLevelCleanUrl=path===`/${lastSegment}`&&canonicalHtmlPages.has(lastSegment);if(isTopLevelCleanUrl){window.location.replace(`/${lastSegment}.html${window.location.search}${window.location.hash}`);}})();const toggle=document.querySelector('.mobile-toggle');const menu=document.querySelector('.main-menu');if(toggle&&menu){toggle.addEventListener('click',()=>{const open=menu.classList.toggle('is-open');toggle.setAttribute('aria-expanded',open?'true':'false');toggle.textContent=open?'✕':'☰';});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.classList.remove('is-open');toggle.setAttribute('aria-expanded','false');toggle.textContent='☰';}));}const nav=document.querySelector('.nav');const onScroll=()=>{if(nav)nav.classList.toggle('is-scrolled',window.scrollY>12);};window.addEventListener('scroll',onScroll,{passive:true});onScroll();const prefersReduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(!prefersReduced&&'IntersectionObserver'in window){const io=new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target);}});},{rootMargin:'0px 0px -8% 0px',threshold:0.08});const autoTargets=['.section-header','.pole-tile','.offer-band','.card','.feature','.person','.article-card','.formation-card','.brand-card','.partner-card','.stat-strip','.villa-band','.contact-box'];document.querySelectorAll(autoTargets.join(',')).forEach((el,i)=>{el.classList.add('reveal');const parent=el.parentElement;if(parent){const siblings=Array.from(parent.children).filter(c=>c.matches(autoTargets.join(',')));const idx=siblings.indexOf(el);if(idx>=0&&idx<4)el.setAttribute('data-d',String(idx));}io.observe(el);});document.querySelectorAll('.reveal:not(.is-visible)').forEach(el=>io.observe(el));}const filterBar=document.querySelector('.resource-filters');const resourceGrid=document.getElementById('resource-grid');if(filterBar&&resourceGrid){const pills=filterBar.querySelectorAll('.filter-pill');const cards=resourceGrid.querySelectorAll('.article-card');const applyFilter=(filter)=>{cards.forEach(card=>{const match=filter==='all'||card.getAttribute('data-cat')===filter;card.classList.toggle('is-hidden',!match);});};pills.forEach(pill=>{pill.addEventListener('click',()=>{pills.forEach(p=>p.classList.remove('is-active'));pill.classList.add('is-active');applyFilter(pill.getAttribute('data-filter'));});});}if(!prefersReduced){const hero=document.querySelector('.hero');if(hero){window.addEventListener('scroll',()=>{const y=Math.min(window.scrollY,600);hero.style.setProperty('--hero-shift',(y*0.06)+'px');},{passive:true});}}
